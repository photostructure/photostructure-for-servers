doctype html
html(lang="en")
  head
    meta(charset="utf-8")
    title #{ title } - Welcome
    meta(name="viewport", content="width=device-width,initial-scale=1")
    link(rel="stylesheet", href="/app/settings.css", nonce=nonce)

  body#app(class=bodyClass)
    main
      if err
        .error.sticky.message
          p= err

      .container.mh100vh.flex-center
        .section.reveal
          form(action="/welcome", method="post") 
            .hero.center.mt4em
              h2 Welcome to
              img.logo(src="/images/logo-horizontal.svg", alt="PhotoStructure")

            .notes
              .info
                i.emj.computer
                | PhotoStructure runs <br> on <dfn tooltip="PhotoStructure is self-hosted: you retain control of all your files.">this computer</dfn>.<br>
                b Your photos and videos <br> stay with you.

              .info
                i.emj.home(title="Your home for all your photos and videos")
                | PhotoStructure is designed to organize <b>all</b> your
                | photos and videos. The initial import may take a while.

              .info
                i.emj.starry-night(
                  title="Let PhotoStructure work while you’re sleeping"
                )
                | You may want to leave your computer running
                | <dfn tooltip="If this is a laptop, make sure it’s plugged in!">overnight</dfn>
                | during your initial import.

              //- p.info
              //-   i.emj.thinking
              //-   | Hover over <br> <dfn tooltip="This is an example of some explanatory text. You’ll find more below ⬇️">underlined text</dfn>
              //-   | or click on links <a href="https://photostructure.com/tag/getting-started/">that look like this</a> for more information.

            .buttons.mt1em
              //- a.btn.skip(href="/settings" title="Don't pick a PhotoStructure plan right now.") skip
              input.btn(
                type="submit",
                title="Choose a PhotoStructure plan",
                value="Continue"
              )
